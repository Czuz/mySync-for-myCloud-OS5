#!/bin/sh
# preinst.sh: This script is used to pre-run some commands if needed, or for
# user to back up their configuration files to other place, such as: /temp,
# /var/tmp or any directory in your hard drive before start installing an App.

exit 0
